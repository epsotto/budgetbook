services:
  db:
    image: postgres
    restart: always
    ports:
      - 5433:5432
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: mysecretpassword
      POSTGRES_DB: budgetbook
    volumes:
      - pgdata:/var/lib/postgresql
volumes:
  pgdata:
